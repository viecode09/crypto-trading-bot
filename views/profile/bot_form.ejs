<div class="p-4">
    <div class="flex gap-6 max-w-[1100px]">
        <%- include('../components/settings_sidebar', { activeSettingsPage }) %>

        <div class="flex-1 min-w-0">
            <div class="flex items-center justify-between mb-4">
                <h1 class="text-2xl font-semibold"><%= isEdit ? 'Edit Bot' : 'New Bot' %></h1>
                <a href="/profiles/<%= profile.id %>" class="text-gray-500 hover:text-gray-700 text-sm">
                    <i class="fas fa-arrow-left mr-1"></i> <%= profile.name %>
                </a>
            </div>

        <div class="bg-white border border-gray-200 rounded max-w-[600px]">
            <form action="/profiles/<%= profile.id %>/bots<%= isEdit ? '/' + bot.id : '' %>" method="POST" class="p-4 space-y-4">
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">Name</label>
                    <input type="text" name="name" required value="<%= bot ? bot.name : '' %>" class="w-full px-3 py-2 border border-gray-300 rounded focus:outline-none focus:ring-1 focus:ring-blue-500" placeholder="e.g., BTC Dip Catcher">
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">Strategy</label>
                    <select id="strategy" name="strategy" required class="w-full px-3 py-2 border border-gray-300 rounded focus:outline-none focus:ring-1 focus:ring-blue-500">
                        <option value="">Select a strategy...</option>
                        <% strategies.forEach(function(s) { %>
                        <option value="<%= s.name %>" data-options='<%= JSON.stringify(s.defaultOptions || {}) %>' <%= bot && bot.strategy === s.name ? 'selected' : '' %>><%= s.displayName %></option>
                        <% }); %>
                    </select>
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">Pair</label>
                    <select id="pair" name="pair" class="w-full" data-slim-select data-slim-placeholder="Search pairs..." required>
                        <option value="">Select a pair...</option>
                        <% pairs.forEach(function(p) { %>
                        <option value="<%= p %>" <%= bot && bot.pair === p ? 'selected' : '' %>><%= p %></option>
                        <% }); %>
                    </select>
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">Interval</label>
                    <select name="interval" required class="w-full px-3 py-2 border border-gray-300 rounded focus:outline-none focus:ring-1 focus:ring-blue-500">
                        <option value="1m" <%= bot && bot.interval === '1m' ? 'selected' : '' %>>1 minute</option>
                        <option value="5m" <%= bot && bot.interval === '5m' ? 'selected' : '' %>>5 minutes</option>
                        <option value="15m" <%= !bot || bot.interval === '15m' ? 'selected' : '' %>>15 minutes</option>
                        <option value="1h" <%= bot && bot.interval === '1h' ? 'selected' : '' %>>1 hour</option>
                        <option value="4h" <%= bot && bot.interval === '4h' ? 'selected' : '' %>>4 hours</option>
                    </select>
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">Capital (USDT)</label>
                    <input type="number" name="capital" required min="1" step="0.01" value="<%= bot ? bot.capital : '' %>" class="w-full px-3 py-2 border border-gray-300 rounded focus:outline-none focus:ring-1 focus:ring-blue-500" placeholder="e.g., 100">
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">Mode</label>
                    <div class="flex gap-4">
                        <label class="flex items-center">
                            <input type="radio" name="mode" value="watch" <%= !bot || bot.mode === 'watch' ? 'checked' : '' %> class="mr-2">
                            <span class="text-sm">Watch (Signals only)</span>
                        </label>
                        <label class="flex items-center">
                            <input type="radio" name="mode" value="trade" <%= bot && bot.mode === 'trade' ? 'checked' : '' %> class="mr-2">
                            <span class="text-sm">Trade (Execute)</span>
                        </label>
                        <label class="flex items-center">
                            <input type="radio" name="mode" value="paper" <%= bot && bot.mode === 'paper' ? 'checked' : '' %> class="mr-2">
                            <span class="text-sm">Paper (Simulate)</span>
                        </label>
                    </div>
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">Strategy Options (JSON)</label>
                    <textarea name="options" id="form-options" rows="6" class="w-full px-3 py-2 border border-gray-300 rounded focus:outline-none focus:ring-1 focus:ring-blue-500 font-mono text-sm" placeholder='{"option": "value"}'><%= bot && bot.options ? JSON.stringify(bot.options, null, 2) : '' %></textarea>
                    <p class="text-xs text-gray-500 mt-1">Optional: Override default strategy options</p>
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">Active</label>
                    <label class="flex items-center">
                        <input type="checkbox" name="status" <%= !bot || bot.status === 'running' ? 'checked' : '' %> class="mr-2">
                        <span class="text-sm">Bot is running</span>
                    </label>
                </div>

                <div class="flex justify-end gap-2 pt-4 border-t border-gray-200">
                    <a href="/profiles/<%= profile.id %>" class="px-4 py-2 text-sm text-gray-600 hover:text-gray-800">Cancel</a>
                    <button type="submit" class="px-4 py-2 text-sm bg-blue-600 text-white rounded hover:bg-blue-700 cursor-pointer"><%= isEdit ? 'Update Bot' : 'Create Bot' %></button>
                </div>
            </form>
        </div>
        </div>
    </div>
</div>

<script>
// Strategy select change handler - update options textarea from data attribute
document.getElementById('strategy').addEventListener('change', function() {
    const selectedOption = this.options[this.selectedIndex];
    const optionsEl = document.getElementById('form-options');

    if (selectedOption && selectedOption.dataset.options) {
        const defaultOptions = JSON.parse(selectedOption.dataset.options);
        if (Object.keys(defaultOptions).length > 0) {
            optionsEl.value = JSON.stringify(defaultOptions, null, 2);
        } else {
            optionsEl.value = '';
        }
    } else {
        optionsEl.value = '';
    }
});
</script>
